rootProject.name = 'afrominga'

def javaVersion = JavaVersion.current()

if (javaVersion != JavaVersion.VERSION_17) {
    throw new GradleException("The Afrominga must be executed with Java 17. Currently executing with Java ${javaVersion.majorVersion}.")
}